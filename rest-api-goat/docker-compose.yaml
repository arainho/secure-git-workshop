version: '3'

services:
  web: 
    build: .
    ports: 
      - '5000:5000'
  py-web:
    build: py-web/Dockerfile
    ports:
      - '8000:8000'
  zap2docker:
    image: owasp/zap2docker-weekly
    ports:
      - "8080:8080"
    environment:
      LEVEL_TO_SHOW: PASS
      REPORT_FILE: api_report.json
      TARGET: openapi.json
    depends_on:
      - web 
    volumes:
      - ./:/zap/wrk
      - ./zap-api-scan.sh:/zap/wrk/zap-api-scan.sh
    entrypoint: /zap/wrk/zap-api-scan.sh

